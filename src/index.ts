export const add = (a: number, b: number): number => a + b;

export const longFormat = (
  a: number,
  b: number,
  c: number,
  d: number,
  e: number,
  g: number,
  q: number,
  h: number,
  v: number,
  u: number,
  e: number
): number => a + b;
